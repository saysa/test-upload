{% extends 'base.html.twig' %}


{% block body %}
<h1>Add a new product</h1>

    {{ form_start(form) }}
        {{ form_row(form.toto) }}
        {{ form_row(form.saysa) }}
    <button type="submit">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
